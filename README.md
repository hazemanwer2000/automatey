# automatey

Automate everything!

## Installation Guide

First, build the Python wheel using `poetry`.

```bash
poetry build
```

Second, install the Python wheel using `pip`.

```bash
pip install *.whl
```
